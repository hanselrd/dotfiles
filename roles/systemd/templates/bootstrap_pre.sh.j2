#!/usr/bin/env sh

counter=0

while true; do
    if [ -S "/tmp/.X11-unix/X0" ]; then
        exit 0
    fi

    : $((counter=counter+1))

    if [ "$counter" -gt 60 ]; then
        exit 1
    fi

    sleep 1s
done
