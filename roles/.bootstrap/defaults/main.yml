---
# defaults file for bootstrap

bootstrap_cache_dir:
bootstrap_configuration:
bootstrap_git_user_email:
bootstrap_git_user_name:
bootstrap_root_dir: /
bootstrap_temp_dir: /tmp
bootstrap_theme: default
bootstrap_user:
bootstrap_user_dir:
