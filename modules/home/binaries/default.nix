{
  config,
  lib,
  pkgs,
  ...
}:
{
  # home.packages = with pkgs; [
  #   dotfiles-cli
  #   # (lib.x.buildGoBin "df-decrypt" { inherit pkgs; })
  #   # (lib.x.buildGoBin "df-encrypt" { inherit pkgs; })
  #   # (lib.x.buildGoBin "df-master-key" { inherit pkgs; })
  # ];
}
